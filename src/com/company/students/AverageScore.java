package com.company.students;

public class AverageScore {
    public void averageScore(int[] a) {
        double averageScore = 0;
        for (int i = 0; i < a.length; i++) averageScore += a[i];
    }
}